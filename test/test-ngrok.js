/* eslint-disable no-console */
/*
const ngrok = require("ngrok");

ngrok.connect(function (err, url) {

    if (err) {
        throw err;
    } else {
        console.log(url);
    }

}); // https://757c1652.ngrok.io -> http://localhost:80 

ngrok.connect(9090, function (err, url) {

    if (err) {
        throw err;
    } else {
        console.log(url);
    }

}); // https://757c1652.ngrok.io -> http://localhost:9090 

ngrok.connect({proto: "tcp", addr: 22}, function (err, url) {

    if (err) {
        throw err;
    } else {
        console.log(url);
    }

}); // tcp://0.tcp.ngrok.io:48590 
*/